<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{description}}</p>
  <p>{{ calories }} calories</p>
  <p>{{ingredients }} </p>

  <p> {{ instructions }}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
</div>
